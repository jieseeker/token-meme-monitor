# Runtime Operability Hardening

Harden local service operations so the worker, scheduled backtest worker, dashboard, logs, and health checks can be controlled and inspected from one predictable workflow.
