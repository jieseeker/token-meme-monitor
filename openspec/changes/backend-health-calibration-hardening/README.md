# Backend Health and Calibration Hardening

Track the service-side hardening added after the first p4 walk-forward run: prediction calibration should ignore weak outcome labels, DexScreener pairs that are not indexed yet should stop consuming every refresh cycle, and operators need a compact health report for data growth and backend queue status.
